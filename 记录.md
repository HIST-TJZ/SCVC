之前的文档里面还提到了各向异性等等的实验，都与这个的单程光测量有关，即公设下的单程光你认为是否有可能出现预测外的结果？
**先把“预测外”分清楚**

“预测外”要看是对谁的预测：

- 对公设本身：公设定义时间坐标，单程光速在该坐标下是确定数，不存在内部意义上的“预测外”。出现意外只说明公设失效了——激光旋转不均匀、圆环/盘面运动不稳、刚体不刚，这些是系统误差，不是新物理。
- 对 SR 的预言：SR 预言的是约定无关的可观测量，也就是原始几何：轨迹是否直、`ΔX` 是否不随方向变化、往返量、红移等。如果原始几何出现方向依赖或弯曲，那才是 SR 预测外。
- 对“单程光速的数值”：SR 根本不预言这个数值，所以测出的数不等于 `c` 不算预测外，只是你选的那套坐标给出的结果。

**所以答案是：有可能，而且这正是文档里 Method A 要探测的东西**

英文文档写得很清楚：如果 `c(θ)` 各向异性，蚀刻轨迹会偏离直线（式 10-11），轨迹弯曲就是正向探测，直线就是上限约束。Method A 的周期锁定法通过相邻段轨迹是否平滑衔接来检测方向依赖；16 个变体把介质、温度、电磁场、引力梯度、旋转等逐一设为受控变量，每个都有标准理论的零假设。

“预测外结果”最干净的形式有三种：

1. 同一公设下重复实验不自洽——可重复性被破坏；
2. 原始几何随装置方向或取向变化——各向异性信号；
3. 某个变体出现超出已知物理的通道偏差，例如单程 Fresnel 拖曳系数与往返值不同，或旋转耦合进光传播。

这些一旦出现且排除了系统误差，就是 SR 的预测外，也就是新物理候选。

**但要小心归因**

装置本身也是物理：旋转、下落、刚体、钟都受同一套动力学支配。如果真有 Lorentz 破坏，它不只改 `c(θ)`，也会改激光的均匀性、盘面的刚性、钟的速率。一个弯曲轨迹只能说明“某处不对”；要把它归因于“光速各向异性”还是“力学/钟异常”，必须靠测试理论（SME 类模型）和交叉变体来隔离。文档里的 same-n 控制组、温度梯度、电磁场、引力梯度等变体，做的正是这件事。

**我的判断**

原则上有可能，但预期偏向 null。现有 SME 约束已经把往返各向异性压到约 `10^-17` 量级，而这份实验在 `λ ~ 0.1–10 m` 的单程通道上设定的灵敏度只有 `|δc|/c ~ 5.8%`。要在这么低的灵敏度下看到正的预测外，需要一个现有约束没覆盖的大效应——单程与往返通道确实互补，所以不是不可能，但文档自己也把 null 作为主要预期、positive 作为“变革性”结果。真出现了，它会是新物理的信号，而不是“公设下测单程光速这个框架错了”。

里面不是说了一个关键点吗？即之前的实验都是往返实验，这样的话可能有几何消去波动，因此这些变种实验专门测量非往返是非常有必要的（因为激光旋转的不可能有反向的光路来往返），你认为是否是这样？

基本是这样，但把“几何消去波动”说准一点：往返实验消掉的是**方向反对称项**，也就是 `c(θ)` 与 `c(θ+π)` 的差。

**为什么往返会消掉**

一段长度 `L` 的往返时间大致是：

```text
t_round = L/c(θ) + L/c(θ+π)
```

如果两个方向的单程速度不同，比如 `c(θ) = c + δ`、`c(θ+π) = c − δ`，那么 `δ` 在和中互相抵消，往返时间只留下偶次项。这正是 Michelson–Morley 只对 `O(v²/c²)` 敏感的原因，也是单程实验能测 `O(v/c)` 一级不对称的原因。往返实验里，所有“方向反对称”的物理——包括一级各向异性和某些反向路径上不对称的介质效应——都被几何平均掉了。

**旋转激光版确实是单程**

这个方案的激光束从发射端走到盘面/圆环就被吸收，没有返回光路，所以 `c(θ)` 和 `c(θ+π)` 的不对称不会被几何抵消。一个原始几何上的弯曲轨迹，或相邻段相位锁定的衔接错位，会直接暴露方向不对称。这就是 Method A 和 16 个变体存在的理由：它们是专门为“非往返”通道设计的，测的是往返实验原则上测不到的那部分。

**但单程实验也有代价**

往返实验的另一个好处是共模消除：同一路径来回两次，路径上的温度、密度、振动等波动对两个方向的影响几乎相同，自动被减掉。单程实验没有这种共模抵消，所以系统误差更难压，任何“预测外”都要先排除介质、机械振动、热梯度、杂散场等。文档里的 same-n 控制组、温度梯度、电磁场、引力梯度等变体，做的正是把通道一个个隔离出来；文档自己也写了这一 caveat。

所以我的回答是：是的，这个必要性判断成立——在测试理论（如 SME 类）框架下，单程变体是往返实验无法替代的互补通道。它检验的是“光传播的方向不对称是否为零”；如果原始几何真的出现可重复、可控的方向依赖，那将是 SR 预测外的新物理信号。

工程问题我们不管，算的不一定对，然后我想知道一点，严格意义上，这是单程光的实验吗？

严格意义上要分两层回答：

**光路层面：是。**

光子从发射端（旋转激光器）到接收端（盘面/圆环）只走一趟，被吸收，没有返回路径，也不依赖返回信号。从光学路径的定义看，这就是严格意义上的单程传播，与 Fizeau、Michelson 那种 A→B→A 的往返实验在路径结构上完全不同。

**物理量层面：不是“严格意义的单程光速测量”。**

“单程光速”在 SR 里不是一个约定无关的物理量；A→B 需要把发射事件和到达事件配成同一时间坐标，这一步只能是公设/约定。所以这个实验测到的原始几何（轨迹形状、`ΔX`）是约定无关的、真实的单程传播效应，但把它换算成一个数值 `c`，那个数值是“你选的坐标下的单程速度”，不是独立于约定的“单程光速”。

因此严格的说法是：

- 它是**单程光的实验**——光路确实只有单程；
- 它不是**单程光速的严格测量**——单程光速本身在 SR 里没有独立于约定的值，任何实验都只能给出约定下的数值。

这也正是英文文档里 Method A 和 Method B 的分工：Method A 测方向不对称（靠原始几何，约定无关），Method B 尝试取绝对值（被同步约定卡住）。如果你问的是“实验里光是不是只走单程”，答案就是是。

问题是，怎么样证明是往返的？都被吸收了，光路也不可逆，无法聚焦在一点上？

分类的标准不是“光子最终有没有被吸收”，而是**实验设计里有没有一条返回光路**。任何实验的光子最后都会被吸收，这不影响它是往返还是单程。

**历史实验怎么证明是往返的**

靠装置构造就能证明：Fizeau 齿轮法、Michelson 旋转镜法、Foucault 法都在远端放了一面反射镜，光束 A→B 后被镜子反射回 A，检测器也在 A。可以直接做个检验：把远端反射镜拆掉或挡住，返回信号立刻消失，测量根本无法完成——这证明时间测量依赖那条返回光路。往返不是事后论证，是光路设计的事实。

**我们的实验怎么证明不是往返**

同样靠构造：接收端是吸收面，没有反射镜，没有返回光路，也没有任何信号回到发射端；盘面/圆环上的蚀刻只记录第一次到达。光被吸收、路径不可逆、没有聚焦返回装置，这些恰恰就是它不是往返的原因。整个装置里不存在第二条从接收端指向发射端的光路。

所以结论很干脆：

- 往返/单程由“测量用的光路有几条腿”决定；
- 历史实验：两腿（A→B→A），因为远端有镜子；
- 这个方案：一腿（A→B），因为远端只有吸收面。

如果你愿意，也可以在圆环或盘面上放一面镜子把光反射回去，那就变成往返实验了——这正说明分类完全由设计决定，而不是由“光能不能逆向聚焦”决定。

问题是，吸收面是否可以释放光子，来光路可逆呢？

能释放，但“释放”不等于“可逆”，更不等于制造出一条往返光路。

**吸收面再发射是什么**

吸收面把光子能量变成激发态，之后可能以荧光、磷光或热辐射的形式重新放出光子。这些再发射光子：

- 有延迟（吸收-再发射有时间常数）；
- 方向随机（自发辐射是各向同性的）；
- 非相干（没有固定相位关系）；
- 波长通常也不同（斯托克斯位移、热谱）。

它们不会沿原光路回到发射端，也不能用来做往返定时：你不知道它什么时候被放出、朝哪个方向走、跟入射光有没有相位关系。所以这种“释放”恰恰是**不可逆**的体现，不是可逆。

**真正可逆需要什么**

要让光路严格可逆，需要一个确定性元件把光束按原路送回，比如镜面反射、角锥反射器，或者光学相位共轭（时间反演）。这些都不是“吸收面”能做到的；吸收面专门是低反射、耗散的。如果给接收端装一面镜子或相位共轭镜，光确实会返回发射端——但那时实验设计就变了，变成往返实验了。

**回到分类**

单程/往返看的是测量用的设计光路。这个方案里，被测量的是第一次到达吸收面的光子；即使表面有一些残余再发射，它不沿原路返回，也不是定时信号的一部分，最多算背景噪声。所以：

- 吸收面释放光子 → 不构成返回光路；
- 没有确定性返回元件 → 不是往返实验；
- 微观量子层面当然存在时间反演振幅，但实验不测量它，统计上也完全淹没。

严格地说，这个实验的光路仍然是单程的。

然后这个实验的时钟又是怎么回事？相当与放在公设上了？

对，就是这个意思：这个实验里的“钟”不是独立测出来的，而是直接被放进了公设里。

具体看它有几层：

- **发射端钟**：激光旋转均匀（`ω` 恒定）是公设。编码器给出角度，但“角度差 = 时间差”用的是 `ω`，而 `ω` 的均匀性本身不是测出来的，是假设的。
- **到达端钟**：圆盘/圆环下落均匀（`v` 恒定）也是公设。径向位置/高度被当成到达时刻，这个“高度 = 时间”的换算完全由 `v` 这个公设支撑。
- **同时性配对**：“圆盘冻结与激光停转是同一时刻”“两块板同高就是同时”，这些也是公设，等价于钟搬运同步。

所以整个实验真正测到的只有几何量：角度、距离、位置差。时间没有独立出现，它完全由“均匀旋转 + 均匀下落 + 同时配对”这三条公设定义。单程光速就是把几何量除以这套公设给出的时间，所以它是**这套坐标下的定义值**，不是独立测量的物理量。

这也正好回答了之前的问题：为什么这个数值与 SR 预言无关——因为 SR 不预言某个公设坐标下的单程速度，而公设本身又决定不了“哪个时间是真的”。原始几何读数（比如轨迹形状、`ΔX`）才是约定无关、真正可测的部分；钟，整个都在公设上。

但是好像确实不是异地钟？这算是同步了钟吗？

对，操作意义上确实没有两台钟，也没有“对表”这个动作——激光旋转是唯一的物理钟。但逻辑上，这个实验仍然在做一个等价于同步的事，只是形态不同。

拆开看：

- **发射端时间轴**：激光旋转角度，由 `ω` 公设支撑。
- **到达端时间轴**：盘面位置（烧蚀点半径）或冻结事件，由 `v` 公设支撑。
- **两者如何连接**：把“盘面在某个位置/冻结”解释成“发射端时间坐标里的某个 t”，必须假设盘面运动和激光旋转属于**同一个实验室时间**。

这个“同一个时间”的假设，就是同步的实质。经典同步是拿两台钟互相对一下，这里是把“到达事件映射到发射端时间”的配对直接写进公设（均匀下落、刚体同时冻结、同高=同时）。所以：

- 你说得对：它**不需要操作意义上的异地钟对表**；
- 但它仍然**隐含地同步**：它用运动学公设规定了两地事件的时间关系。

所以严格的说法是：这不是“两台钟的同步”，而是“用一个公设把到达端事件投影到发射端时间坐标上”。功能上和同步等价，只是没有钟的样子。这也正是为什么接受公设后测量完全可行、结果却是该坐标下的值：同步问题没有消失，它变成了公设。

但是如果在另一个宇宙，这个实验发现测量的光速是有大有小的，不是误差导致的，那么会怎么样？

那就要看变化是什么形式，但共同结论是：**这套实验会测出一个 SR 无法容纳的结果，也就是新物理。**

**先分几种“有大有小”**

- 方向依赖：不同朝向测出的值不同 → 各向异性，存在优选方向。
- 时间依赖：同一装置不同时刻测出不同值 → 光速有隐藏变量，或真空状态在演化。
- 条件依赖：温度、介质、电磁场、引力梯度改变测量值 → 光传播有新的耦合通道。
- 运动状态依赖：装置相对某个绝对参考系运动时数值改变 → 存在优选系/以太式结构。

**会发生什么**

第一，SR 在这个宇宙里就只是近似或直接被推翻：光速不再是普适常数，而是方向、条件或时间的函数。光锥结构也随之改变，因果极限可能变成方向性的：有些方向信号极限大于另一些方向。

第二，往返实验仍然可能测不出来：如果变化是方向反对称的（`c(θ) ≠ c(θ+π)`），往返时间会把差异抵消，只剩偶次项。这正是“单程实验必要”的另一种表述——你描述的那种宇宙，恰好是历史实验永远测不到、这个方案专门去照的那一面。

第三，也是最重要的 caveat：这个实验的钟在公设上。如果光速真的变了，钟（激光旋转、圆盘下落、原子跃迁）很可能也同时受同一个隐藏变量影响。所以判断依据必须是**原始几何**——轨迹是否弯、`ΔX` 是否随方向/时间变化——而不是换算出来的那个数。原始几何变了，就是硬信号；只有换算数变了，还可能是钟的公设失效。

**哲学后果很大**

如果变化是系统的、和某个优选系绑定的，那么“同时”就不再只是约定，而可能成为物理事实：那个宇宙有真实的绝对时间结构，单程光速变成客观可测的物理量，Reichenbach 的约定论在那个宇宙里不再适用。爱因斯坦的 `ε = 1/2` 同步会变成一个“坐标选择”，而不是“定义”。

所以“另一个宇宙里光速有大有小”意味着：光速不是常数、Lorentz 对称破坏、可能引入优选系甚至绝对时间。这不是实验出错，而是教科书级别的新物理——也正好是这个单程实验设计真正要探测的东西。

这意思是，这个实验仍然有可能会出现新的结果，不一定受SR的约束？

是，但要精确说是哪一层“新结果”。

**原始几何层面：是。**

SR 对原始几何有明确预言：同一装置下轨迹应该是直线，`ΔX` 不随方向、时间、装置取向变化，往返量遵循标准关系。如果实验出现可重复、排除系统误差的偏差——轨迹弯曲、`ΔX` 方向依赖、某个变体出现异常耦合——那就是 SR 预测外的结果，意味着 Lorentz 对称性可能被破坏，或者光传播有新的耦合通道。这正是这个实验作为“检验”的意义，而且不循环：原始几何是约定无关的。

**换算出的单程数值层面：不是。**

换算出来的 `c` 不是 SR 的预言对象，它随公设变化。所以“测出的数不等于某个预期值”本身不是预测外，那只是你选的坐标；SR 的约束根本不作用在那个数上。

**所以完整表述是：**

这个实验仍然可能出现 SR 约束不了、甚至违背 SR 的新结果，但新结果必须落在**原始几何**上，而不是落在“单程光速等于多少”上。SR 能约束的是约定无关的可测部分；单程数值那部分是公设定义的，SR 本来就不管它。

在我们的宇宙里，现有 SME 约束把往返各向异性压到约 `10^-17`，所以偏差大概率测不到；但单程、米级波长这个通道还没被现有实验覆盖过，原则上它确实可以给出 SR 之外的结果。这正是这套设计的价值所在。