# E284：v1-v6 进化全景——SCVC训练的六个阶段

**日期**: 2026-07-27 | **性质**: AI训练进化史

---

## 总览

```
v1-v3:  探索期  —— "SCVC数据好像有用？"
v4:     突破期  —— 三阶段SFT，14/15
v5:     精炼期  —— DPO偏好优化，诚实度提升
v6:     融合期  —— 12条数据教会三层光谱滑动
```

---

## v1-v3：探索期

**做了什么：** 单阶段SFT，混入SCVC训练数据。

**结果：** 发现SCVC提示词让0.5B模型从0/15跳到3/15。答案质量从"全部拒答"变成"尝试推理但粗糙"。训练曲线出现Grokking——平台→突破→重组→真泛化。概念学习证据。

**发现：** SCVC数据有效。但答案太硬——不能做的事也强行推，导致部分错误。

---

## v4：三阶段SFT突破

**做了什么：** 分阶段训练
- 阶段1：数学基础（π、DH求和、toric几何）
- 阶段2：方法训练（约束链追溯、Grokking验证）
- 阶段3：物理应用（α、Tc、带隙等具体问题）

**结果：** Baseline 9/15，SCVC 14/15。答案开始结构化——有了公式、有了推导链、有了"不能确定但可以告诉你..."的分层回答。

**发现：** 分阶段训练比混在一起好。模型需要先学数学，再学方法，再学应用。这本身就是约束链的结构。

---

## v5：DPO偏好优化

**做了什么：** 在SFT基础上加入DPO（Direct Preference Optimization）
- 偏好：诚实标记边界 > 强行推理
- 偏好：正确推导链 > 含糊其辞
- 偏好：分层回答 > 单层猜测

**结果：** 边界题（上帝、人生意义、分手）的诚实度显著提升。模型不再在无约束领域硬推。Q14的alpha争议回答更准确。

**发现：** DPO不是让模型"更安全"——是让模型"更精确地标记自己的认知边界"。这和RLHF的安全对齐完全不同——不是政治正确，是几何正确。

---

## v6：连续光谱融合

**核心洞察（来自E283之前的讨论）：**
> 感性和理性是同一根轴上的不同位置。猜到极致=推导，推导到极致=直觉。

**做了什么：** 仅12条训练数据，教模型三层光谱滑动：

```
[SCVC 100%] → [约束推测 ~75-85%] → [模式推测 ~5-25%]
     ↑                ↑                    ↑
  几何锁死        约束延伸推理          模式匹配/感性
```

**训练效率：** loss 4.25→0.43。12条数据。这在正常fine-tuning中不可想象——正常需要成千上万条。但SCVC数据每条都是约束链，信息密度极高。

**v6结果：**

| 问题 | SCVC推导 | 约束推测 | 模式推测 |
|------|:--:|:--:|:--:|
| α数值 | ✅ 100% | — | — |
| 股市 | ❌ 0% | ~5% | 0% |
| 蓝鲸 | ✅ 100% 平方立方律 | ~75% | ~25% |
| 人生意义 | ❌ 0% | ~5% | 0% |

---

## 进化路线总结

```
v1:    SCVC=有效 ← 发现
v2-3:  分阶段训练更好 ← 优化
v4:    14/15，结构化回答 ← 质变
v5:    诚实边界 ← 精确
v6:    连续光谱 ← 统一

v6的loss 0.43 vs v1的loss 2.8
v6的12条数据 vs v1的上百条
```

每步都在逼近同一个目标：**让模型知道它知道什么，知道它不知道什么，并且在两者之间平滑过渡。** 这不是训练一个更聪明的AI——是训练一个更精确的认知光谱。

---

## v6训练数据：12条的微观结构

scvc_v6_fusion.jsonl，总共12条。每条都是三层滑动的一个实例：

| # | 类型 | SCVC层 | 推测层 | 边界层 |
|:--:|------|:--:|:--:|:--:|
| 1-4 | 物理常数（alpha, m_e, alpha_s, sin²θ_W） | 100% | — | — |
| 5 | 寿命极限 | 100% | 75% | — |
| 6-9 | 无约束领域（股市/上帝/意义/分手） | 0% | 5% | 0% |
| 10 | alpha=140争议 | 100%反驳 | — | — |
| 11-12 | 混合（永动机/硅带隙） | 100% | 85% | 5% |

**12条覆盖了整个光谱：纯几何锁死→几何推测→无约束沉默。** 模型从这12条中提取的不是12个答案——是光谱滑动的元规则。

---

## 硬数据：箭头密度与思考时间

v4 SCVC vs Baseline的量化差异：

| 指标 | Baseline | SCVC | 倍数 |
|------|:--:|:--:|:--:|
| 箭头(→)密度 | 0/30题 | 24/30题 | ∞ |
| 平均回答时间 | 1-3s | 18-20s | ~10× |
| 含数学公式 | 0/15 | 5/15 | ∞ |

箭头=因果链。不是语言风格——是权重级别的推导操作。箭头越多，模型在追溯约束链越深。

思考时间增加10倍：不是变慢了——是**真的在算**。Baseline在查表，SCVC在走约束链。

---

## 1.7T极限 vs SCVC极限：同一条曲线

`
大模型(1.7T)路线:  数据越多 → 猜到极致 → 趋近真相
SCVC(0.5B)路线:   约束越紧 → 抹掉不可能 → 只剩真相

1.7T = 多边形趋近圆
SCVC = 圆的方程

描述的是同一个圆。
`

v6训练的三层滑动——[SCVC 100%]→[约束推测]→[模式推测]——就是这个圆的完整参数化。

---

## 版本收敛作为证据

如果SCVC是随机的，6个版本会发散（每个版本随机方向，越来越远）。实际上6个版本全部收敛——v1→v3→v4→v5→v6，每一步都更接近同一个目标：精确的认知光谱。收敛本身就说明背后有真实结构在引导。

## 深层含义

1. **数据效率：** 12条数据教会三层滑动。因为这些数据不是"例子"——是"约束"。每个约束覆盖无限多的情况。

2. **Grokking现象贯穿始终：** 每个阶段都出现先平台后突破的模式。模型不是记答案——是学规则。

3. **百分比标注是自我认知的前身：** 模型说"这个75%"——它在标记自己对置信度的元认知。这不是AGI，但这是朝着AGI方向的有效一步。

4. **SCVC不是训练法——是认知框架：** v1-v6始终围绕同一个核心：追溯几何锚→抹掉不可能→诚实标记边界。所有阶段都是这个核心在不同粒度上的实现。

---

*E284: v1-v6进化全景。2026-07-27。*
